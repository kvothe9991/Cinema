@model IEnumerable<Cine.Models.Socio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Puntos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatosPersonales)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioDescuento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Puntos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatosPersonales)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioDescuento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SocioID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SocioID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SocioID })
        </td>
    </tr>
}

</table>
